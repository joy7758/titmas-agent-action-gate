# MCP boundary

This directory contains a server manifest only. No server is implemented or deployed in milestone 1.

The Action Gate MCP server has no provider credentials and its six tools do not mutate GitHub. A future provider MCP remains a separate trust domain and accepts only exact, server-verified, unexpired, unconsumed `ALLOW` decisions plus its own identity/ACL checks.
